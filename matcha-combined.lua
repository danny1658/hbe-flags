-- Matcha Combined Script for Luau with Type Annotations\n-- Script includes Desync, Fast Animations, and Hitbox Expander features\n\n-- Define type for Vector3\ntype Vector3 = { x: number; y: number; z: number }\n\n-- Sample function to create a Vector3\nlocal function newVector3(x: number, y: number, z: number): Vector3\n    return { x = x, y = y, z = z }\nend\n\n-- Function for Desync\nlocal function performDesync()\n    -- logic for desync implementation here\nend\n\n-- Function for Fast Animations\nlocal function enableFastAnimations()\n    -- logic for fast animations\nend\n\n-- Function for Hitbox Expander\nlocal function expandHitbox(hitboxSize: Vector3)\n    -- logic for hitbox expansion\nend\n\n-- Initialization function\nlocal function init()\n    performDesync()\n    enableFastAnimations()\n    expandHitbox(newVector3(5, 5, 5)) -- example hitbox size\nend\n\n-- Run initialization\ninit()\n